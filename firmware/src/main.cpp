// MaizAI — Sensor Node Firmware
// To be populated from the firmware specification.

#include <Arduino.h>

void setup() {
    Serial.begin(115200);
    Serial.println("MaizAI sensor node — scaffold only");
}

void loop() {
    delay(1000);
}
