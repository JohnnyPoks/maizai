export type {
  User,
  LeafImage,
  Classification,
  SensorReading,
  Recommendation,
  RuleThreshold,
  Session,
} from "@prisma/client";

export { Role, DiseaseClass, InferenceSource, UrgencyLevel, SyncStatus } from "@prisma/client";
